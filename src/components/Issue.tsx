import React from 'react'

type Props = {}

//will we receive here an issue id and search for it && present it here?

const Issue: React.FC<{}> = (props) => {
  return (
    <div>Issue</div>
  )
}

export default Issue